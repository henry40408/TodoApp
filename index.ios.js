import React, {
  AppRegistry,
  View,
} from 'react-native';

class TodoApp extends React.Component {
  render() {
    return (<View />);
  }
}

AppRegistry.registerComponent('TodoApp', () => TodoApp);
